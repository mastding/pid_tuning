# PID 智能整定系统 - 应用功能文档

## 系统概述

PID 智能整定系统是一个基于多智能体协同架构的工业控制回路 PID 参数自动整定平台。系统通过数据分析、系统辨识、PID 参数整定和闭环评估等多个智能体的协作，实现 PID 控制参数的自动化、智能化整定。

---

## 核心功能模块

### 1. 智能整定功能

#### 1.1 数据输入方式
- **CSV 文件上传**：支持上传本地 CSV 格式的历史数据文件
  - 自动检测时间列（支持 `timestamp`、`time`、`datetime`、`date`、`test` 等多种列名）
  - 自动识别 SV/PV/MV 列
  - 支持多回路数据识别
  - 支持时间范围选择，可按时间段切分 CSV 数据
  
- **历史数据获取**：直接从外部系统 API 获取历史数据
  - 支持指定时间范围
  - 自动数据格式转换

#### 1.2 回路类型支持
系统支持以下四种回路类型的 PID 整定：

| 回路类型 | 英文标识 | 典型应用场景 |
|---------|---------|-------------|
| 流量 | `flow` | 管道流量控制、物料输送 |
| 温度 | `temperature` | 反应釜温度、换热器控制 |
| 压力 | `pressure` | 储罐压力、管道压力控制 |
| 液位 | `level` | 储罐液位、分离器液位控制 |

#### 1.3 多智能体协作流程

系统包含 4 个核心智能体，协同完成整定任务：

**① 数据分析智能体**
- 职责：数据读取、清洗、预处理
- 功能：
  - CSV/历史数据读取
  - 列名标准化（支持中英文列名映射）
  - 时间戳解析（支持多种时间格式）
  - 缺失值处理
  - 候选阶跃事件检测
  - 生成候选辨识窗口

**② 系统辨识智能体**
- 职责：过程模型辨识与选择
- 功能：
  - 多模型拟合（FO/FOPDT/SOPDT/IPDT）
  - 模型参数估计
  - 模型质量评估
  - 最优模型选择

**③ PID 专家智能体**
- 职责：PID 参数整定
- 功能：
  - 基于模型类型选择整定策略
  - 历史经验检索与复用
  - 生成初始 PID 参数
  - 参数细调优化

**④ 评估智能体**
- 职责：闭环仿真评估
- 功能：
  - 闭环响应仿真
  - 性能指标计算
  - 通过/不通过判定
  - 回流优化触发

---

### 2. 系统辨识功能

#### 2.1 支持的模型类型

| 模型 | 标识 | 参数 | 适用场景 |
|-----|------|------|---------|
| 一阶模型 | `FO` | K, T | 快速流量、压力控制 |
| 一阶加纯滞后 | `FOPDT` | K, T, L | 一般工业过程 |
| 二阶加纯滞后 | `SOPDT` | K, T1, T2, L | 复杂动态过程 |
| 积分加纯滞后 | `IPDT` | K, L | 液位、积分过程 |

#### 2.2 模型选择机制
系统辨识智能体会综合考虑以下因素选择最优模型：
- 拟合误差（RMSE）
- 决定系数（R²）
- 模型置信度
- 后续 PID 试算效果
- 回路类型先验知识

#### 2.3 候选窗口机制
系统不会直接使用整段历史数据，而是：
1. 检测数据中的阶跃变化事件
2. 为每个阶跃事件生成自适应辨识窗口
3. 在多个候选窗口上分别尝试辨识
4. 选择最优窗口和模型组合

---

### 3. PID 整定功能

#### 3.1 按模型类型的整定策略

**FO 模型整定**
- 整定方法：FO 保守 Lambda / IMC
- 特点：适合快流量、快压力、弱滞后对象

**FOPDT 模型整定**
- 整定方法：
  - IMC（内模控制）
  - LAMBDA（Lambda 整定法）
  - ZN（Ziegler-Nichols）
  - CHR（Chien-Hrones-Reswick）

**SOPDT 模型整定**
- 整定方法：
  - SOPDT 主导/次级时间常数近似整定
  - 优先 Lambda / IMC
  - 受限条件下可尝试保守版 ZN / CHR

**IPDT 模型整定**
- 整定方法：积分过程专用 Lambda / IMC
- 特点：按积分过程真实模型仿真

#### 3.2 整定参数输出
系统输出以下 PID 参数：
- `Kp`：比例增益
- `Ki`：积分时间常数
- `Kd`：微分时间常数
- 整定方法说明
- 模型参数

---

### 4. 闭环评估功能

#### 4.1 仿真评估机制
系统基于辨识的过程模型和整定的 PID 参数进行闭环仿真，评估控制效果。

#### 4.2 性能指标

**第一层：性能分（Performance Score）**
基于闭环仿真提取以下指标：
- 超调量（权重 25%）
- 调节时间（权重 20%）
- 稳态误差（权重 25%）
- 振荡次数（权重 15%）
- 衰减比（权重 15%）

**第二层：方法置信度（Method Confidence）**
来自系统辨识阶段的模型置信度。

**第三层：综合评分（Final Rating）**
```
final_rating = 0.7 × performance_score + 0.3 × (method_confidence × 10)
```

**通过阈值**：`final_rating >= 7.0`

#### 4.3 自动回流优化
如果首次评估不通过，系统会自动分析原因并回流优化：

**回流给 PID 专家智能体**
- 适用情况：超调过大、振荡明显、参数偏激进
- 优化动作：自动收紧 Kp/Ki，重新评估

**回流给系统辨识智能体**
- 适用情况：模型可信度不足、当前候选窗口不理想
- 优化动作：切换其他候选窗口，重新辨识、整定和评估

---

### 5. 经验中心功能

#### 5.1 经验沉淀
每次整定任务完成后，系统自动生成结构化经验记录：
- 回路信息（类型、URI）
- 数据来源与时间范围
- 原始模型类型与参数
- 初始 PID 与最终 PID
- 初始评估与最终评估
- 是否经过自动细调
- 优化参数（refine_delta, refine_gain）
- 经验总结与标签

#### 5.2 经验存储
- **JSONL 文件**：事实源存储
- **SQLite 数据库**：检索索引

#### 5.3 相似经验检索
系统支持按以下条件检索相似经验：
- 回路类型匹配
- 模型类型匹配
- 模型参数相似度（K, T, L, T1, T2 等）
- 历史案例成功率
- 历史评分高低

#### 5.4 经验复用
经验不会直接替代闭环试算，而是用于：
- 给出历史偏好策略
- 给出历史偏好模型
- 给出推荐的 Kp/Ki/Kd 收紧比例
- 优先生成"历史修正版 PID"候选

---

### 6. 前端页面功能

#### 6.1 智能整定页（tuning.html）
- 发起整定任务
- 选择数据来源（CSV/历史数据）
- 选择回路类型
- 上传 CSV 文件
- 时间范围选择（开始时间/结束时间）
- 实时查看多智能体执行过程
- 查看各工具调用与结果
- 查看最终整定结果与评估
- 查看历史经验参考摘要

#### 6.2 经验中心页（experience.html）
- 查看经验统计概览
- 按条件筛选经验（回路类型、模型类型、策略、是否通过）
- 相似经验检索
- 查看单条经验详情
- 清除全部历史经验
- 重建经验检索索引

#### 6.3 回路分析页（loop-analysis.html）
- 回路数据分析
- 阶跃事件检测可视化
- 辨识窗口展示
- 模型拟合效果对比

#### 6.4 案例库页（case-library.html）
- 查看内置整定案例
- 案例详情展示
- 案例学习参考

---

### 7. API 接口功能

#### 7.1 工作流接口

**提交整定任务**
```
POST /api/agent/workflow/run
```
- 支持异步/同步/流式三种响应模式
- 返回任务 ID 用于后续查询

**查询任务状态**
```
GET /api/agent/workflow/status/{task_id}
```
- 实时获取任务执行进度

**查询任务结果**
```
GET /api/agent/workflow/result/{task_id}
```
- 获取整定最终结果

#### 7.2 CSV 处理接口

**CSV 回路识别**
```
POST /api/tuning/csv/inspect-loops
```
- 自动识别 CSV 中的控制回路

**CSV 窗口识别**
```
POST /api/tuning/csv/inspect-windows
```
- 识别候选阶跃事件和辨识窗口
- 返回时间范围信息

**CSV 时间切分**
```
POST /api/tuning/csv/slice-by-time
```
- 按指定时间范围切分 CSV 数据
- 支持自动时间列检测

#### 7.3 经验中心接口

**获取经验列表**
```
GET /api/experiences
```

**搜索相似经验**
```
POST /api/experiences/search
```

**删除经验**
```
DELETE /api/experiences/{experience_id}
```

**清空所有经验**
```
POST /api/experiences/clear-all
```

---

### 8. 技术特性

#### 8.1 多模型辨识
- 同时尝试多种过程模型
- 基于综合指标选择最优模型
- 不再简单将所有模型统一为 FOPDT

#### 8.2 自适应窗口
- 根据阶跃幅度自适应调整窗口大小
- 前导 padding：max(30, min(180, amplitude × 18))
- 后导 padding：max(180, min(900, amplitude × 45))

#### 8.3 鲁棒性处理
- 自适应去噪（弱/中/强/自动）
- 缺失值插值处理
- 异常值检测与处理
- 多编码格式支持（UTF-8/GBK/GB18030）

#### 8.4 实时反馈
- SSE（Server-Sent Events）实时推送执行过程
- 前端实时展示多智能体协作流程
- 每个工具调用的输入输出可视化

---

### 9. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层                                │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐        │
│  │  智能整定页   │ │  经验中心页   │ │  回路分析页   │        │
│  └──────────────┘ └──────────────┘ └──────────────┘        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        API 层                               │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐        │
│  │ 工作流接口    │ │ CSV 处理接口  │ │ 经验中心接口  │        │
│  └──────────────┘ └──────────────┘ └──────────────┘        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     多智能体编排层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 数据分析  │ │ 系统辨识  │ │ PID 专家 │ │ 评估智能体│       │
│  │  智能体   │ │  智能体   │ │  智能体  │ │          │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      服务层                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 数据服务  │ │ 辨识服务  │ │ 整定服务  │ │ 评估服务  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      存储层                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │ 经验 JSONL│ │ SQLite   │ │ 任务状态  │                    │
│  └──────────┘ └──────────┘ └──────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

---

### 10. 使用场景

#### 10.1 典型应用场景
1. **新建回路整定**：为新的控制回路生成初始 PID 参数
2. **回路优化**：对现有回路进行重新整定优化
3. **故障诊断**：通过模型辨识分析回路动态特性变化
4. **经验沉淀**：积累整定经验，形成知识库
5. **培训学习**：通过案例库学习整定方法和技巧

#### 10.2 适用行业
- 石油化工
- 电力能源
- 冶金制造
- 食品饮料
- 制药行业
- 水处理

---

## 总结

PID 智能整定系统通过多智能体协同、多模型辨识、闭环仿真评估和经验沉淀等核心技术，实现了工业控制回路 PID 参数的自动化、智能化整定。系统具有以下核心优势：

1. **智能化**：自动数据分析和模型辨识，无需人工干预
2. **多模型**：支持多种过程模型，适应不同回路特性
3. **可解释**：多智能体协作流程可视化，结果可追溯
4. **自学习**：经验自动沉淀，支持相似经验复用
5. **高可靠**：闭环仿真评估，自动回流优化

---

*文档版本：1.0*  
*最后更新：2026-04-14*
